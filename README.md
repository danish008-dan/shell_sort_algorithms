# shell_sort_algorithms
Shell Sort is an in-place comparison-based sorting algorithm. It is an optimization over Insertion Sort that allows elements to move across larger distances using a gap sequence.
